{% extends 'base.html.twig' %}
{% block title %}Créer un produit{% endblock %}

{% block body %}
    <div class="container">
    <h1>Créer un produit</h1>
     {{ form_start(form) }}
        {{ form_widget(form) }}
    {{ form_end(form) }}
    <a class="button-common button-blue" href="{{ path('product_list') }}">Retour</a>
    </div>
{% endblock %}